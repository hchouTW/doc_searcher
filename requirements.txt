# Document Searcher (本機多格式文件內文關鍵字檢索系統)
# 適用環境：Python 3.10–3.14 (Windows, macOS, Linux)
# pyproject.toml 為依賴的唯一來源；此檔僅供尚未改用 pyproject 的建置腳本使用，請同步修改。

# 核心介面 (跨平台純 Python GUI)
PySide6>=6.6.0

# 文件內容提取函式庫 (無 OCR，輕量快速)
pymupdf>=1.24.0
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0
xlrd>=2.0.1
olefile>=0.46

# 中文/英文混合分詞
jieba>=0.42.1
